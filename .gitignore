# --- Python / uv ---
.venv/
__pycache__/
.ipynb_checkpoints/

# --- dbt (regenerated by dbt deps / dbt run) ---
target/
dbt_packages/
logs/
.user.yml

# --- Local DuckDB dev warehouse (rebuilt by dbt build) ---
# Unlike last project, NOT committed for Cloudflare: prod Evidence reads
# the Fabric Warehouse or parquet exports (project doc §6), not this file.
*.duckdb
*.duckdb.wal

# --- Evidence (regenerated by npm install / npm run sources / build) ---
evidence/node_modules/
evidence/.evidence/
evidence/.svelte-kit/
evidence/build/
evidence/static/data/
*.options.yaml

# --- Secrets (service principal creds, Cloudflare deploy hook URL) ---
.env

# --- Local data cache (PG texts live in the Lakehouse, not the repo) ---
data/

# --- Old-project reference material (Fiction-Fingerprint), not part of this repo ---
corpus/
extract/

# --- OS noise ---
Thumbs.db
desktop.ini
