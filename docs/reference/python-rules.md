# Python rules

No exceptions:

- `from __future__ import annotations` first.
- Annotate params and returns.
- No `import *`.
- Data objects: `dataclasses`/`NamedTuple`.
- f-strings only.
- Modules under 300 lines.
- No bare `except:`/`except Exception:`.
