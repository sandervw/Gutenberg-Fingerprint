diff --git a/node_modules/@evidence-dev/evidence/template/src/pages/+layout.js b/node_modules/@evidence-dev/evidence/template/src/pages/+layout.js
index a47b4f6..4c44738 100644
--- a/node_modules/@evidence-dev/evidence/template/src/pages/+layout.js
+++ b/node_modules/@evidence-dev/evidence/template/src/pages/+layout.js
@@ -126,7 +126,8 @@ export const load = async ({ fetch, route, params, url }) => {
 	if (!browser && dev) await initDB();
 
 	// let SSR saturate the cache first
-	if (browser && isUserPage && prerender) {
+	// patched: hydrate prerendered pages even in SPA mode (404 -> {} for non-prerendered)
+	if (browser && isUserPage) {
 		data = await getPrerenderedQueries(routeHash, paramsHash, fetch);
 	}
 
