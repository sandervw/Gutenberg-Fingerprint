snapshots:
  - name: snap_dim_work
    relation: ref('dim_work')
    config:
      unique_key: work_key
      strategy: check
      check_cols: all
      # Works dropped from dim_work close out instead of staying open
      hard_deletes: invalidate
